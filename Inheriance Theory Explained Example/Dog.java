public class Dog extends Animal {

  public Dog() {
      super("Mutt", "Big", 50.0);
  }

    // The Dog class is a subclass (child) of Animal,
    // which means it inherits the attributes and methods of the Animal class.
    // This constructor calls the Animal constructor (using super) with
    // specific values for a dog.
    // The values set type to "Mutt", size to "Big", and weight to 50.0.
    // Since Dog extends Animal, it inherits all methods and fields from Animal.

}
